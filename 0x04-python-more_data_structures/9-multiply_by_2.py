#!/usr/bin/python3
def multiply_by_2(a_dictionary):
     if multiply_by_2 is not None:
         b_dict = dict(a_dictionary)
         for key, values in b_dict.items():
             b_dict[key] = values * 2
         
         return b_dict
